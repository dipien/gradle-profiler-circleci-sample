buildscript {
	repositories {
		jcenter()
	}

	buildscript {
		dependencies {
			classpath(BuildLibs.KOTLIN_GRADLE_PLUGIN)
		}
	}
}

allprojects {
	repositories {
		jcenter()
	}
}

wrapper {
	distributionType = Wrapper.DistributionType.ALL
}
